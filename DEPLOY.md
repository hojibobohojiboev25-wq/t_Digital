# 🚀 Развертывание проекта T.Digital

## 🌐 Vercel Deployment (Рекомендуется)

Проект настроен для автоматического развертывания на **Vercel**.

### Шаг 1: Подключение GitHub к Vercel

1. Перейдите на https://vercel.com
2. Нажмите "Import Project"
3. Выберите "From Git Repository"
4. Подключите ваш GitHub аккаунт
5. Выберите репозиторий `t_Digital`

### Шаг 2: Настройка переменных окружения

В разделе "Environment Variables" добавьте:

```
TELEGRAM_BOT_TOKEN=ваш_токен_бота
TELEGRAM_CHAT_ID=ваш_chat_id
```

### Шаг 3: Развертывание

Нажмите "Deploy" - Vercel автоматически:
- ✅ Определит тип проекта
- ✅ Установит зависимости
- ✅ Соберет и развернет сайт
- ✅ Предоставит URL (например: `t-digital.vercel.app`)

---

## 🔧 Локальная разработка

### Запуск сервера:
```bash
# Установка зависимостей (если нужно)
npm install

# Запуск локального сервера
node simple-server.js

# Откройте http://localhost:8080
```

### Админ-панель:
- URL: http://localhost:8080/admin/login
- Логин: `admin`
- Пароль: `admin123`

---

## 📊 Структура развертывания

### Vercel Functions:
- `index.js` - Сервер статических файлов
- `api/contact.js` - Обработка контактных форм
- `api/lang.js` - Многоязычная поддержка

### Статические файлы:
- HTML страницы
- CSS стили
- JavaScript
- Изображения
- Переводы

---

## ⚙️ Переменные окружения

### Обязательные для продакшена:
```bash
# Telegram Bot (для уведомлений)
TELEGRAM_BOT_TOKEN=your_bot_token
TELEGRAM_CHAT_ID=your_chat_id

# Админ-панель
ADMIN_USERNAME=admin
ADMIN_PASSWORD_HASH=your_hashed_password
```

### Опциональные:
```bash
NODE_ENV=production
PORT=8080
```

---

## 🔍 Проверка развертывания

### После развертывания проверьте:

1. **Главная страница** - Загружается ли сайт?
2. **Многоязычность** - Переключаются ли языки?
3. **Контактная форма** - Приходит ли сообщение в Telegram?
4. **Админ-панель** - Работает ли вход?
5. **Мобильная версия** - Адаптивный ли дизайн?

---

## 🚨 Устранение проблем

### Если Vercel показывает ошибку:

1. **Проверьте `vercel.json`** - Правильная ли конфигурация?
2. **Проверьте переменные** - Все ли env переменные заданы?
3. **Логи сборки** - Посмотрите ошибки в Vercel dashboard

### Если форма не отправляется:

1. **Telegram токен** - Правильный ли токен?
2. **Chat ID** - Корректный ли chat_id?
3. **API routes** - Работают ли `/api/contact` и `/api/lang`?

---

## 🔄 Обновление сайта

### Автоматическое обновление:
После `git push` в main ветку Vercel автоматически:
- Пересоберет проект
- Развернет новую версию
- Обновит сайт

### Ручное обновление:
В Vercel dashboard нажмите "Redeploy"

---

## 💡 Советы по оптимизации

### Для продакшена:
- Включите HTTPS (автоматически в Vercel)
- Настройте домен
- Добавьте аналитику (Google Analytics)
- Настройте мониторинг ошибок

### SEO оптимизация:
- Проверьте `robots.txt`
- Обновите `sitemap.xml` с реальным доменом
- Добавьте meta теги для социальных сетей

---

## 🎯 Финальный результат

После успешного развертывания у вас будет:

- 🌐 **Рабочий сайт** на Vercel
- 📱 **Адаптивный дизайн**
- 🌍 **Многоязычная поддержка**
- 🤖 **Telegram уведомления**
- 🔐 **Админ-панель**
- 📊 **SEO оптимизация**

**URL вашего сайта:** `https://t-digital.vercel.app` (или ваш кастомный домен)

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи Vercel
2. Проверьте переменные окружения
3. Проверьте GitHub Actions (если настроены)

**Проект готов к продакшену! 🚀**